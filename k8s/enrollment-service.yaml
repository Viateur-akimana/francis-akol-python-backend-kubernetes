apiVersion: apps/v1
kind: Deployment
metadata:
  name: enrollment-service
  namespace: mlh-platform
  labels:
    app: enrollment-service
    app.kubernetes.io/name: enrollment-service
    app.kubernetes.io/part-of: modular-learning-hub
spec:
  replicas: 2
  selector:
    matchLabels:
      app: enrollment-service
  template:
    metadata:
      labels:
        app: enrollment-service
    spec:
      containers:
      - name: enrollment-service
        image: mlh-enrollment-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8003
          name: http
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: mlh-secrets
              key: ENROLLMENT_DATABASE_URL
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: mlh-config
              key: REDIS_URL
        - name: CELERY_BROKER_URL
          valueFrom:
            configMapKeyRef:
              name: mlh-config
              key: CELERY_BROKER_URL
        - name: CELERY_RESULT_BACKEND
          valueFrom:
            configMapKeyRef:
              name: mlh-config
              key: CELERY_RESULT_BACKEND
        - name: USER_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: mlh-config
              key: USER_SERVICE_URL
        - name: COURSE_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: mlh-config
              key: COURSE_SERVICE_URL
        - name: ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: mlh-config
              key: ENVIRONMENT
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8003
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: enrollment-service
  namespace: mlh-platform
  labels:
    app: enrollment-service
spec:
  type: ClusterIP
  selector:
    app: enrollment-service
  ports:
  - port: 80
    targetPort: 8003
    protocol: TCP
    name: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: enrollment-service-hpa
  namespace: mlh-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: enrollment-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
