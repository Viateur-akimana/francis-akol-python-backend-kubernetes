[build]
builder = "dockerfile"
dockerfilePath = "../../../Dockerfile.course-service"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
